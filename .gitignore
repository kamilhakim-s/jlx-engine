# Gradle
.gradle/
build/

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
*.code-workspace

# OS
.DS_Store

# Java / JFR / profiling output
*.class
*.jfr
*.hprof
*.log

# Project data (downloaded datasets, journals). Unanchored so it matches a `data/`
# directory at the repo root or under any module (e.g. market-data/data/ created when
# a demo runs with the module as its working directory).
data/

# Frontend (dashboard-web) build + deps
dashboard-web/node_modules/
dashboard-web/dist/
